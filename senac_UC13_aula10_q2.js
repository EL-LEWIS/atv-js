/**
 * Este Arrey pega 5 elementos e substitue o segundo elemento e exibindo os elementos atualizado
 * 
 * 
 * @param {string[]} stocks
 * 
 * 
 * @return {Object} 
 */

const stocks = ['appel', 'appelsource', 'orange', 'limon'];

stocks[1] = 'abacaxi';

console.log(stocks.length);

console.log(stocks);
